{
  "name": "@f/is-generator",
  "description": "Check if generator.",
  "repository": "git://github.com/micro-js/is-generator.git",
  "version": "1.3.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
