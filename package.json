{
  "name": "@micro-js/is-generator-function",
  "description": "Check if generator function.",
  "repository": "git://github.com/micro-js/is-generator-function.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "is-generator": "^1.0.2"
  },
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
